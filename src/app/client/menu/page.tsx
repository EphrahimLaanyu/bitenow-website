import { ClientFeaturePlaceholder } from "@/features/client/components/client-feature-placeholder";

export default function ClientMenuPage() {
  return (
    <ClientFeaturePlaceholder
      description="Guest-facing menu browsing, categories, item details, and add-to-cart actions will live here."
      title="Client Menu"
    />
  );
}
